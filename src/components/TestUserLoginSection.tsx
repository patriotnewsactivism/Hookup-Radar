// Preview builds intentionally use the same authentication surface as production.
// A reusable browser-bundled credential is not a safe preview mechanism.
export function TestUserLoginSection() {
  return null;
}
